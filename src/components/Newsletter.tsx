"use client";
import { FormEvent, useState } from "react";
export function Newsletter() { const [message, setMessage] = useState(""); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const email = new FormData(e.currentTarget).get("email"); const res = await fetch("/api/newsletter", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email }) }); setMessage(res.ok ? "Votre inscription est confirmée. À très bientôt." : "Veuillez utiliser une adresse e-mail valide."); } return <section className="newsletter"><div><p className="eyebrow">Newsletter TAMUSNI</p><h2>La revue dans votre boîte mail.</h2><p>Une synthèse utile, une fois par semaine. Aucune sollicitation superflue.</p></div><form onSubmit={submit}><label htmlFor="email">Votre adresse e-mail</label><div><input id="email" name="email" type="email" required placeholder="vous@exemple.com"/><button>S’inscrire</button></div>{message && <p className="form-message">{message}</p>}</form></section>; }
