import Link from "next/link"; import { articles } from "@/lib/content";
export default function FocusPage(){return <section className="format-page shell"><p className="eyebrow">Format</p><h1>FOCUS</h1><p className="intro">Les analyses qui donnent le contexte, expliquent les mécanismes et mesurent les conséquences.</p><div className="article-list">{articles.filter(a=>a.format === "FOCUS").map(a=><article key={a.slug}><div className="list-image" style={{background:a.image}}/><div><p className="meta"><span>{a.category}</span> · {a.readingTime}</p><Link href={`/article/${a.slug}`}><h2>{a.title}</h2></Link><p>{a.excerpt}</p></div></article>)}</div></section>}
