import Link from "next/link"; import { articles } from "@/lib/content";
export default function FlashPage(){return <section className="format-page shell"><p className="eyebrow">Format</p><h1>FLASH</h1><p className="intro">Les informations essentielles, vérifiées et publiées sans bruit inutile.</p><div className="article-list">{articles.filter(a=>a.format === "FLASH").map(a=><article key={a.slug}><div className="list-image" style={{background:a.image}}/><div><p className="meta"><span>{a.category}</span> · {a.date}</p><Link href={`/article/${a.slug}`}><h2>{a.title}</h2></Link><p>{a.excerpt}</p></div></article>)}</div></section>}
