import { notFound } from "next/navigation";
import Link from "next/link";
import { articles } from "@/lib/content";
import { AdSlot } from "@/components/AdSlot";
export default async function ArticlePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const article = articles.find(item => item.slug === slug); if (!article) notFound(); return <article className="article-page shell"><header><p className="meta"><span>{article.category}</span> · {article.format} · {article.readingTime}</p><h1>{article.title}</h1><p className="article-deck">{article.excerpt}</p><p className="byline">Par la rédaction TAMUSNI · Publié le {article.date}</p></header><div className="article-cover" style={{ background: article.image }}/><div className="article-layout"><aside className="takeaway"><h2>À retenir</h2><ul>{article.takeaway.map(item => <li key={item}>{item}</li>)}</ul><small>Niveau de lecture : Intermédiaire</small></aside><div className="article-body">{article.body.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}<AdSlot placement="article-in-body"/><h2>Sources et méthodologie</h2><p>TAMUSNI privilégie les sources primaires, organismes publics et publications originales. Les liens ci-dessous permettent de vérifier les éléments présentés.</p><ul className="source-list">{article.sources.map((source) => <li key={source.url}><a href={source.url} target="_blank" rel="noreferrer">{source.label} ↗</a></li>)}</ul><Link className="text-link" href={`/rubrique/${article.category.toLowerCase()}`}>Voir les autres articles {article.category}</Link></div></div><AdSlot placement="article-end" className="ad-horizontal"/></article>; }
