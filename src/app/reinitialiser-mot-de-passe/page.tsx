"use client";
import { FormEvent, useState } from "react";
import { useSearchParams } from "next/navigation";
export default function ResetPassword() { const params = useSearchParams(); const [message, setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const password = new FormData(event.currentTarget).get("password"); const response = await fetch("/api/auth/reset-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ token: params.get("token"), password }) }); const data = await response.json(); if (response.ok) window.location.href = data.redirect; else setMessage(data.error); } return <section className="auth-page shell"><div><p className="eyebrow">Nouveau mot de passe</p><h1>Sécurisez votre compte.</h1><p>Choisissez un mot de passe d’au moins 6 caractères.</p></div><div className="auth-box"><form onSubmit={submit}><label>Nouveau mot de passe<input name="password" type="password" minLength={6} required/></label><button className="primary-button">Enregistrer</button></form>{message && <p className="form-message error">{message}</p>}</div></section>; }
