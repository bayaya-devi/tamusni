"use client";
import { FormEvent, useState } from "react";
export default function ForgotPassword() { const [message, setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const email = new FormData(event.currentTarget).get("email"); await fetch("/api/auth/forgot-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email }) }); setMessage("Si cette adresse est associée à un compte, un e-mail de réinitialisation vient d’être envoyé."); } return <section className="auth-page shell"><div><p className="eyebrow">Accès au compte</p><h1>Réinitialiser votre mot de passe.</h1><p>Nous vous enverrons un lien valable 30 minutes.</p></div><div className="auth-box"><form onSubmit={submit}><label>Adresse e-mail<input name="email" type="email" required placeholder="vous@exemple.com"/></label><button className="primary-button">Envoyer le lien</button></form>{message && <p className="form-message">{message}</p>}</div></section>; }
