(function(){var slug=document.body.dataset.forumSlug;function el(tag,text){var node=document.createElement(tag);if(text!==undefined)node.textContent=text;return node}async function load(){var response=await fetch('/api/forums/'+encodeURIComponent(slug));var data=await response.json();var box=document.getElementById('forum-posts');box.innerHTML='';if(!data.posts.length){box.append(el('p','Aucun message publié. Soyez la première personne à participer.'));return}data.posts.forEach(function(item){var post=el('article');post.className='post';post.append(el('strong',item.name),el('p',item.body),el('small',new Date(item.created_at).toLocaleString('fr-FR')));box.append(post)})}var form=document.getElementById('post-form');if(form)form.onsubmit=async function(event){event.preventDefault();var textarea=form.querySelector('textarea');var response=await fetch('/api/forums/'+encodeURIComponent(slug),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({body:textarea.value})});var data=await response.json();document.getElementById('post-message').textContent=data.message||data.error;if(response.status===401)location.href='/connexion/?next='+encodeURIComponent(location.pathname);if(response.ok)textarea.value=''};load()})();
