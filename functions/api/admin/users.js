import { cleanText, json, readBody, requireAdmin, sameOrigin } from "../../_lib/auth.js";
export async function onRequestGet(context){const admin=await requireAdmin(context);if(!admin)return json({error:"Accès réservé à l’administration."},403);const result=await context.env.DB.prepare("SELECT id,name,email,role,created_at FROM users ORDER BY created_at DESC LIMIT 200").all();return json({items:result.results||[]})}
export async function onRequestPatch(context){if(!sameOrigin(context.request))return json({error:"Origine refusée."},403);const admin=await requireAdmin(context);if(!admin)return json({error:"Accès réservé à l’administration."},403);const body=await readBody(context.request);const id=cleanText(body.id,160);const role=body.role==="ADMIN"?"ADMIN":"USER";if(!id||id===admin.sub)return json({error:"Modification interdite."},400);await context.env.DB.prepare("UPDATE users SET role=? WHERE id=?").bind(role,id).run();await context.env.DB.prepare("INSERT INTO admin_audit_log(id,admin_user_id,action,target_type,target_id,metadata,created_at) VALUES(?,?,?,?,?,?,?)").bind(crypto.randomUUID(),admin.sub,"user.role","user",id,JSON.stringify({role}),new Date().toISOString()).run();return json({ok:true})}
